import * as React from 'react';
import { Text, View, StyleSheet,Image,onpress,scrollview ,image} from 'react-native';
const Alt = (props) => {
return(
  <View style={{flex:props.FlexX, backgroundColor:props.backgroundColorX}}>
  
 <Image></Image>
</View>
);
}
 export default Alt;